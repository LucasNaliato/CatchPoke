<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bag - CatchPoke</title>
    <link rel="icon" href="/images/icon.png">
    <link rel="stylesheet" href="/css/bulma.min.css">
    <link rel="stylesheet" href="/css/pokemon.css">
</head>
<body>
    <div class="bag-main">
        <div class="container">
            <div class="columns mt-6">
                {{#each pokemons}}
                {{#if this.name}}
                <div class="column">
                    <div class="card-bag">
                        <h2 class="card-title">{{this.name}}</h2>
                        <img class="card-test my-6" src='{{this.sprites.animated}}' />
                        <a href="http://localhost:3000/remove/{{this.name}}"><button class="button is-danger">Soltar pokémon</button></a>
                    </div>
                </div>
                {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="container mt-1">
            <a href="http://localhost:3000/"><button class="button is-medium is-link ml-6">Ir pra página home</button></a>
            <a href="http://localhost:3000/wild"><button class="button is-medium is-link ml-6">Procurar pokémon selvagem</button></a>
        </div>
    </div>
</body>
</html>